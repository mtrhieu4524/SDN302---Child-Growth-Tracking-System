import { useEffect } from "react";

const Register = () => {
    useEffect(() => {
        document.title = "Child Growth Tracking - Sign Up";
    }, []);
    return (
        <div>
            register page
        </div>
    );
};

export default Register;
