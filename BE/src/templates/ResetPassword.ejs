<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Password Reset</title>
    <style>
      body {
        background-color: #f5f0e1; /* White Beige */
        font-family: "Open Sans", Arial, sans-serif;
        margin: 0;
        padding: 0;
      }

      .container {
        width: 100%;
        padding: 20px;
      }

      .email-wrapper {
        width: 100%;
        max-width: 400px;
        background: white;
        padding: 30px;
        text-align: left;
        border-radius: 10px;
        border: 1px solid #0c0e0c; /* Black border */
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        overflow: hidden;
      }

      h1 {
        color: #333;
        margin-bottom: 10px;
      }

      p {
        color: #666;
        font-size: 14px;
        line-height: 1.8;
      }

      .header-title {
        margin: 0;
      }

      /* Centering the PIN code properly using a table */
      .pin-table {
        margin: 20px auto;
        border-spacing: 10px;
      }

      .digit-box {
        padding: 10px 20px;
        background-color: #f5f0e1;
        border-radius: 10px;
        text-align: center;
        vertical-align: middle;
      }
      .digit-box p {
        margin: 0;
        font-size: 24px;
        font-weight: bold;
        color: #0c0e0c;
      }
    </style>
  </head>
  <body>
    <table class="container" role="presentation" width="100%" cellspacing="0" cellpadding="0">
      <tr>
        <td align="center">
          <div class="email-wrapper">
            <h1 class="header-title">Reset Your Password</h1>
            <p style="border-top: 1px solid #f5f0e1; padding-top: 20px;">
              Hello <span style="font-weight: bold;"><%= name %></span>,
            </p>
            <p>Use the 6-digit code below to verify your password reset request.</p>
            
            <!-- Using a table to ensure proper centering -->
            <table class="pin-table" role="presentation">
              <tr>
                <td class="digit-box"><p><%= pin[0] %></p></td>
                <td class="digit-box"><p><%= pin[1] %></p></td>
                <td class="digit-box"><p><%= pin[2] %></p></td>
                <td class="digit-box"><p><%= pin[3] %></p></td>
                <td class="digit-box"><p><%= pin[4] %></p></td>
                <td class="digit-box"><p><%= pin[5] %></p></td>

              </tr>
            </table>

            <p style="border-bottom: 1px solid #f5f0e1; padding-bottom: 20px">
              If you did not request this, you can ignore this email.
            </p>
          </div>
        </td>
      </tr>
    </table>
  </body>
</html>
