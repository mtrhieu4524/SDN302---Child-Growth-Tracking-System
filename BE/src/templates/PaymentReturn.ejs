<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Status</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 2rem;
            max-width: 600px;
            width: 100%;
            text-align: center;
        }

        h1 {
            color: #333;
            margin-bottom: 1.5rem;
            font-size: 2rem;
        }

        h2 {
            color: #555;
            margin-top: 1.5rem;
            margin-bottom: 1rem;
            font-size: 1.5rem;
        }

        p {
            color: #666;
            margin: 0.5rem 0;
            font-size: 1rem;
        }

        strong {
            color: #333;
        }

        a {
            color: #007bff;
            text-decoration: none;
            font-weight: bold;
        }

        a:hover {
            text-decoration: underline;
        }

        .success-message {
            color: #28a745;
            font-weight: bold;
            margin-bottom: 1.5rem;
        }

        .failure-message {
            color: #dc3545;
            font-weight: bold;
            margin-bottom: 1.5rem;
        }

        .receipt-details {
            background-color: #f8f9fa;
            border-radius: 6px;
            padding: 1rem;
            margin-top: 1rem;
            text-align: left;
        }

        .receipt-details p {
            margin: 0.5rem 0;
        }

        .button {
            display: inline-block;
            margin-top: 1.5rem;
            padding: 0.75rem 1.5rem;
            background-color: #EF6351;
            color: #fff;
            border-radius: 4px;
            text-decoration: none;
            font-size: 1rem;
            transition: background-color 0.3s ease;
        }

        .button:hover {
            background-color: #6D466B;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Payment Status</h1>
        <p class="<%= success === true ? 'success-message' : 'failure-message' %>">
            <%= message || 'An unexpected error occurred.' %>
        </p>

        <% if (success) { %>
            <h2>Receipt</h2>
            <div class="receipt-details">
                <p><strong>Transaction ID:</strong> <%= receipt?.transactionId || 'N/A' %></p>
                <p><strong>Membership Package ID:</strong> <%= receipt?.packageId || 'N/A' %></p>
                <p><strong>Amount:</strong> <%= receipt?.totalAmount?.value || 'N/A' %> <%= receipt?.totalAmount?.currency || '' %></p>
                <p><strong>Payment Method:</strong> <%= receipt?.paymentMethod || 'N/A' %></p>
                <p><strong>Payment Gateway:</strong> <%= receipt?.paymentGateway || 'N/A' %></p>
            </div>
            <a href="<%= frontendUrl || '/' %>" class="button">Return to Home</a>
        <% } else { %>
            <p>Payment failed. Please try again.</p>
            <a href="<%= frontendUrl || '/' %>" class="button">Go Back</a>
        <% } %>
    </div>
</body>
</html>
